<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neusoft.emanage.regsister.persist.UserRegsistDao">
	<insert id="regsistInsert" parameterType="UserRegsistBean"
		useGeneratedKeys="true" keyProperty="useraccess_id">
		insert into useraccess
		(RMB,GSC,userId,status,phone,email,user_code,idCard,accessPassword,versionNum)
        values(#{RMB},#{GSC},#{userid},#{status},#{phone},#{email},#{user_code},#{idCard},#{accessPassword},#{versionNum})

	</insert>
</mapper>
